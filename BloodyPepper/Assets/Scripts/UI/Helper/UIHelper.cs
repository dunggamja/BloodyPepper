using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UIHelper
{
    ////UI의 움직임을 객체형태로 관리한다. 
    //// : (이동, 색상 조정, 텍스트 & 스프라이트 Set 등)
    //public abstract class UICommand
    //{
    //    public IEnumerator PlayStart()
    //    {
    //        yield return RunCommand();
    //        if(null != onComplete) onComplete();
    //    }

    //    public abstract IEnumerator RunCommand();
    //    public System.Action onComplete = null;
    //}

    //public static class Utils
    //{
    //    Vector2 GetSizeofWord(UnityEngine.UI.Text text, string word)
    //    {
    //        if (null == text || string.IsNullOrEmpty(word))
    //            return Vector2.zero;
    //        foreach(char ch in word)
    //        {
    //            CharacterInfo charInfo;
    //            if(ch == ' ')
    //            {

    //            }
    //        }
    //    }
    //};
}
